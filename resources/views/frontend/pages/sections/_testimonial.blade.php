<section class="testimonial-section fix section-bg section-padding">    <div class="container">        <div class="section-title text-center">            <span class="sub-title justify-content-center wow fadeInUp">                @if (!empty($data['title_bar_image']))		            <img src="{{ asset($data['title_bar_image']) }}" alt="img">	            @endif	            {{ $data['subheading'][$locale] ?? '' }}            </span>	        	        @if (!empty($data['heading'][$locale]))		        <h2 class="wow fadeInUp" data-wow-delay=".3s">			        {{ $data['heading'][$locale] }}		        </h2>	        @endif        </div>	    	    <div class="row g-4">		    <div class="swiper testimonial-swiper">			    <div class="swiper-wrapper">				    @foreach($repeatedContents as $repeatedContent)					    @php						    $contentData = $repeatedContent->content_data ?? [];					    @endphp					    <div class="swiper-slide">						    <div class="testimonial-card-item">							    <div class="star">								    @for ($i = 1; $i <= 5; $i++)									    @if (($contentData['rating'] ?? 0) >= $i)										    <i class="fa-solid fa-star"></i>									    @else										    <i class="fa-regular fa-star"></i>									    @endif								    @endfor							    </div>							    <p>{{ $contentData['comment_text'][$locale] ?? '' }}</p>							    <div class="client-info">								    @if (!empty($contentData['client_image']))									    <img src="{{ asset($contentData['client_image']) }}" alt="client">								    @endif								    <div class="content">									    <h5>{{ $contentData['client_name'][$locale] ?? '' }}</h5>									    <span>{{ $contentData['client_position'][$locale] ?? '' }}</span>								    </div>							    </div>						    </div>					    </div>				    @endforeach                </div>			    <div class="swiper-dot mt-5">				    <div class="dot swiper-pagination"></div>                </div>            </div>        </div>    </div></section>