<section class="service-section fix section-padding section-bg" id="service">    <div class="container">        <div class="section-title-area">            <div class="section-title">                <span class="sub-title wow fadeInUp">                    @if (!empty($data['title_bar_image']))		                <img src="{{ asset($data['title_bar_image']) }}" alt="title-bar">	                @endif	                {{ $data['subheading'][$locale] ?? '' }}                </span>	            	            @if (!empty($data['heading'][$locale]))		            <h2 class="wow fadeInUp" data-wow-delay=".3s">			            {{ $data['heading'][$locale] }}		            </h2>	            @endif            </div>	        	        <div class="array-button wow fadeInUp" data-wow-delay=".5s">		        <button class="array-prev"><i class="fa-solid fa-arrow-left"></i></button>		        <button class="array-next"><i class="fa-solid fa-arrow-right"></i></button>            </div>        </div>	    	    <div class="service-wrapper">            <div class="swiper service-slider">                <div class="swiper-wrapper">	                @foreach($repeatedContents as $repeatedContent)		                @php			                $contentData = $repeatedContent->content_data ?? [];		                @endphp		                		                <div class="swiper-slide">			                <div class="service-item">				                @if (!empty($contentData['service_image']))					                <div class="service-image">						                <img src="{{ asset($contentData['service_image']) }}" alt="service-img">					                </div>				                @endif				                				                <div class="service-content">					                @if (!empty($contentData['service_title'][$locale]))						                <h4>{{ $contentData['service_title'][$locale] }}</h4>					                @endif					                					                @if (!empty($contentData['service_text'][$locale]))						                <p>{!! nl2br(e($contentData['service_text'][$locale])) !!}</p>					                @endif				                </div>			                </div>		                </div>	                	                @endforeach                </div>            </div>        </div>    </div></section>