<li class="d-lg-inline-block d-none">    <div class="user cursor-pointer ">        <img class="d-inline-block rounded-circle" src="{{ asset(auth()->user()->avatar_alt) }}" height="40" alt="img">        <h6 class="d-md-inline-block d-none mb-0">{{ auth()->user()->name }}</h6>        <div class="author-card">            @if(auth()->user()->rank)                <h6 class="names border-bottom">                    <img src="{{ asset(auth()->user()->rank->icon) }}" alt="img" height="30">                    <a href="{{ route('user.rank.showcase') }}">                        {{ __(':rank Member', ['rank' => auth()->user()->rank->name]) }}                    </a>                </h6>            @endif            <div class="list-details">                <a href="{{ route('user.settings.profile') }}">                    <span>                        <x-icon name="user-cog"/>                    </span>                    {{ __('My Settings') }}                </a>                <a href="{{ route('user.notifications.index') }}">                    <span>                        <x-icon name="notification"/>                    </span>                    {{ __('Notification') }}                </a>                <a href="{{ route('user.wallet.index') }}">                    <span>                        <x-icon name="wallet-2"/>                    </span>                    {{ __('My Wallets') }}                </a>                <a class="border-bottom" href="{{ route('user.support-ticket.index') }}">                    <span>                        <x-icon name="ticket"/>                    </span>                    {{ __('Support Ticket') }}                </a>                {{-- Logout Action --}}                <a href="#" onclick="event.preventDefault(); document.getElementById('logout-form').submit();">                    <span>                        <x-icon name="logout"/>                    </span>                    {{ __('Logout') }}                </a>                <form id="logout-form" method="POST" action="{{ route('logout') }}">                    @csrf                </form>            </div>        </div>    </div></li>