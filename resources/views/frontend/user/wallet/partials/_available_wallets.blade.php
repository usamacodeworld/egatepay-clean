<option value="" disabled selected>{{ __('Select Wallet') }}</option>@if(isset($wallets) && $wallets->count() === 1)    @php $wallet = $wallets->first(); @endphp    <option value="{{ $wallet->id }}">        {{ __(':name Wallet - :balance', ['name' => $wallet->name, 'balance' => $wallet->currency->symbol . $wallet->balance]) }}    </option>@elseif(isset($wallets) && $wallets->count() > 1)    @foreach($wallets as $wallet)        <option value="{{ $wallet->id }}">            {{ __(':name Wallet - :balance', ['name' => $wallet->name, 'balance' => $wallet->currency->symbol . $wallet->balance]) }}        </option>    @endforeach@else    <option value="" disabled>{{ __('No available wallets found') }}</option>@endif